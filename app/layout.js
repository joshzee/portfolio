import { Montserrat } from 'next/font/google'
import './globals.css'

const montserrat = Montserrat({ subsets: ['latin'] })

export const metadata = {
  title: 'Josh Codes',
  description: 'A Web Developer from Melbourne, Australia. Building Websites, React Apps, Showing the utilising of Container Technologies and Cloud Services and More',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={montserrat.className}>{children}</body>
    </html>
  )
}
